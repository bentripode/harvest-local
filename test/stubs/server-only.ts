// vitest alias target for the `server-only` package, whose real default export throws when
// imported outside a React Server Component. Our server modules are plain functions under test.
export {};
